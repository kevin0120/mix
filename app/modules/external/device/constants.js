export const deviceType = Object.freeze({
  controller: 'controller',
  scanner: 'scanner',
  tool: 'tool',
  reader: 'reader',
  io: 'io'
});

export const status2Healthz = Object.freeze({
  online: true,
  offline: false
});
