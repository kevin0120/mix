const styles = (theme) => ({
  root: {
    display: 'flex',
    width: '100%',
    height: '100%',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center'
  },
  desc: {
    marginBottom: '50px',
    display: 'flex',
    flexDirection: 'row'
  }
});

export default styles;
