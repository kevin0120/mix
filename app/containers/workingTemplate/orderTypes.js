import Working from '../working';
import StepWorking from '../stepWorking';

export default {
  screw:{
    container:Working,
  },
  step:{
    container:StepWorking
  },
  default:{
    container:StepWorking
  }
}
